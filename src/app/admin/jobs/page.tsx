// TODO(admin-ui engineer): jobs list (ADMIN_UX.md §5). Title, slug, active
// toggle, candidate counts by stage, assessment config name.
export default function AdminJobsPage() {
  return (
    <main className="p-8">
      <h1 className="text-xl font-semibold">משרות</h1>
      <p className="mt-2 text-neutral-500">
        רשימת המשרות תיבנה כאן — ראו ADMIN_UX.md §5.
      </p>
    </main>
  );
}
