// TODO(assessment-engine engineer): speed templates go here (ASSESSMENT_DESIGN.md §3).
// One file per template family, e.g. speed.<name>.ts, each exporting a
// { id, version, render(seed), score(answer) } template object consumed by
// src/assessment/generator.ts. Kept as a placeholder file so this directory
// is tracked by git before any templates exist.
export {};
