// TODO: typed SQL query modules go here (ARCHITECTURE.md §4), e.g.
// `applications.ts`, `assessment.ts`, `admin.ts` — one file per aggregate,
// each built on withCandidate/withAdmin/withSystem from ../postgres.ts.
// Owned by whichever engineer builds the first feature that needs a query
// (candidate-flow for applications, assessment-engine for sessions/items,
// admin-ui for the list/detail views).
export {};
